# 黄金裔工作台 · 差距报告（2026-09-04）

## 1. 总评

一句话：**骨架对了，血肉还没长上。** 设计里最重要的两条用户裁决——「以 dsh-synapse 为基底吸收改造、不另起画布」（WB-01/06）和「注册为 conversation.view 第二 Tab、不用覆盖层」（WB-08/45）——都已落地：synapse 的画布、一问一答卡、分支、追问、检查器、贝塞尔连线全部保留（WB-02/04/42/44），postMessage 桥接协议已按 `amphoreus:*` 迁移并做了同源校验（WB-10/47/52-57），席位归属也已由 `bindings` 表唯一权威持久化（WB-22/69）。但从「用户打开工作台」这一刻感受到的东西看，差距集中在四处：① 技能注入的 `<system-reminder>`／`<skill_content>` 原文被当成用户提问投影成卡片（破坏体验）；② 宿主页不向 iframe 转发任何主题 token，画布永远是 synapse 原版白底蓝点，与逐席配色割裂（阻塞逐席风格）；③ vendored 画布不带 nonce，点「归档」等写操作会直接 403；④ 卡片上完全没有「谁承办、哪条是移交」的黄金裔语义，进席后看到的只是「按席过滤的 synapse 原版」。此外还有一条架构级偏离：会话正文仍在宿主侧截断并落盘 `workbench.json`、经无令牌的自定义路由外露（SEC-1），与设计「正文只走浏览器 ctx.sessions」正好相反。总体估算：M2 应交付项约完成 **四成**；M3 项（移交边、站位轨、台账、接通中尾页）按计划尚未开工，属正常。

## 2. 已达成

| ID | 内容 | 关键证据 |
|---|---|---|
| WB-02 / WB-37 | 投影规则继承：一问一答一卡、tool 按 callId 折进助手消息、fork 以 atSeq 锚定、子会话只投影 firstLiveSeq 之后、session/event 微任务批处理 | app.js:686-781；src/host/workbench.ts:435-480；src/index.ts:70-93 |
| WB-04 / WB-42 | synapse 交互层全部保留：追问、分支、草稿卡、快捷词、选区追问、检查器、详情视图、视口虚拟化、pointer 拖动、贝塞尔连线、折叠子树 | app.js:12, 52-60, 331-347, 555-613, 783-873, 1097-1192, 1444-1492, 1558-1606 |
| WB-06 / WB-46 | index.js→H8、client.js→B6、app.js+styles.css→包内 `workbench/` vendoring，由 H5 静态服务；`files` 白名单含 `workbench/**` | src/host/webapi.ts:21, 213-224, 429-436；package.json:27 |
| WB-08 | 以 ui-trajectory 形状注册 `conversation.view` 第二视图，不再 appendChild 覆盖层 | src/client/index.ts:62-83；workbench.tsx:140-155 |
| WB-10 / WB-47 | 桥接 source 改 `dsh-amphoreus`，双向 origin 校验（SEC-5） | workbench.tsx:47-60；app.js:124-126, 1850 |
| WB-52 / 54 / 56 / 57 | 宿主页→iframe 五条消息、iframe→宿主页 map-ready/request-current/activate-session/fork-session/send-message 全部保留 | workbench.tsx:63-112；app.js:356, 397, 1857, 1907-1911 |
| WB-44 | synapse 自带 markdown 渲染（表格/代码/行内，Map 缓存 5000）在 iframe 内保留 | app.js:457-539 |
| WB-22 / WB-69 | 归属唯一权威 = 域 `amphoreus` 表 `bindings`；不写自定义会话事件、不落 localStorage | store.ts:30-39, 120-141；webapi.ts:266-308 |
| WB-51 | `amphoreus:map-opened` 由 Tab 激活触发 —— 终判 **done**（初判 partial 被反驳）：官方 ConversationSession 以 `{only: active.id}` 渲染，非激活视图即卸载，故 iframe onLoad 与 Tab 激活一一对应。注意这是**隐式依赖**官方卸载行为，未写显式监听 | workbench.tsx:147-152 |

## 3. 差距清单（按「打开工作台立刻感受到」排序）

> 状态口径：**缺失** = 没做；**部分** = 做了一半；**偏离** = 做了但方向与设计相反。同一问题的重复条目已合并列出。

### 第一梯队：破坏体验 / 阻塞逐席风格

**G1 · 注入消息漏成卡片** — WB-01（子项）／WB-15
- 状态：偏离
- 差在哪：`injector.ts:38-41` 以 user message 注入技能卡全文；`src/host/workbench.ts:576-578` 的 `projectableEvent` 对 user/message 只过滤 `'Current runtime context…'` 前缀（606-608），不识别 skill-invocation 来源；`458-460` 还用这段文本生成线程标题；app.js 侧亦无过滤。
- 用户感受：进席后画布第一张卡标题就是 `<system-reminder> A skill is a reusable set…` 和 `<skill_content name="amphoreus-cyrene">…`（浏览器实测），像是自己问了一段乱码。
- 来源：05 §1.2 行35；00 §6.1 行597
- 建议里程碑：**M2 立即修**

**G2 · 画布拿不到主题 token，永远白底** — WB-11／WB-50／WB-32
- 状态：缺失
- 差在哪：宿主页 `workbench.tsx` 出站集合无 `amphoreus:theme` 也无 `amphoreus:theme-tokens`，不监听 `theme/change`，不读 `getComputedStyle` 上的 `--dsw-alias-*`；`styles.css:2,4` 写死 `#1f2937/#f5f7fa`，`#3478f6` 30 处，165 条 `[data-theme="dark"]` 规则全是独立 hex，`--dsw-alias-*` 0 命中；`app.js:1859-1860` 有接收端但无人喂。设计明确否决了「只传 dark 布尔」这一备选，现状连这个都没有。
- 用户感受：DSH 切暗色、进不同席，iframe 里始终是 synapse 原版浅色系统字体；与外壳金紫杂志配色明显是两个产品。
- 来源：00 §6.2 行604/611、§6.4 行633；02 §7 改造2 行55；HANDOFF §4 行88（自认「待 token 化」）
- 建议里程碑：**M2 立即修**

**G3 · 画布写操作必定 403** — WB-19／WB-66
- 状态：部分（Host+nonce 门已做，但画布自身不带 nonce）
- 差在哪：`webapi.ts:485-492` 对所有非 GET 请求要求 `x-amphoreus-nonce === this.nonce`；但 vendored `app.js:117-122` 的 `api()` 只带 content-type，全文无 `__AMPHOREUS_BOOT__`/nonce 字样 → `app.js:287` DELETE 归档、`:399` POST branch 记录都会被拒。
- 用户感受：点「归档」或分支后弹红条 `invalid amphoreus nonce`。
- 来源：00 §2.7 行260-261；HANDOFF 行104
- 建议里程碑：**M2 立即修**（几行代码：读 `__AMPHOREUS_BOOT__.nonce` 并加头）

**G4 · iframe 顶部多一条「对话/工作台」切换条，且「对话」按钮无效** — WB-53／WB-12（close）／WB-45（争议）
- 状态：WB-53 缺失；WB-45 部分 **⚠ 核查有分歧**
- 差在哪：`app.js:1364` 仍渲染 `<header class="topbar"><div class="view-switch">…<button data-action="close">对话</button>`；点击发 `amphoreus:close`，宿主页 `workbench.tsx:107-109` 直接 `return`（注释 nothing to close）；`index.ts:25` 未注入 `uiConversation`，无 `activate('chat')`。
- 用户感受：两层重复的 Tab 条并存，上面那条点了没反应。
- ⚠ 分歧：**维持方**认为 iframe 内切换条与官方 Tab 重复且无功能，WB-45「删除切换条」未完成；**反驳方**认为 WB-45 点名要删的是 synapse client.js 注入宿主页 `document.body` 的 `.dsh-synapse-switch` 顶栏（02:24），那个确已删除；iframe 内的 `.view-switch` 是设计预期保留的 `amphoreus:close` 触发点（00:612 规定 close → 切回「对话」Tab），故 WB-45 应为 done，真正缺口是 WB-53 的 close 语义。两方都同意：**用户可见的问题是一样的**——要么让「对话」按钮真的切回 Tab，要么把它藏掉。
- 来源：00 §6.2 行612、§6.4 行632；02 §4 行24
- 建议里程碑：**M2**

**G5 · 卡片没有黄金裔身份：无承办徽记、无席色、无名牌** — WB-21／WB-67／WB-23
- 状态：部分（席位过滤到位，卡片级全缺）；WB-23 缺失
- 差在哪：`app.js:907-916` conversationCard 输出固定 `--thread-color:#3478f6`，meta 只有「DSH 会话/分支/追问 · 第 N 轮」；`WorkbenchThread` 无 skillName/face 字段（workbench.ts:48-62）；`styles.css:52` 还用 `!important` 把侧栏 tree-row 的席色覆盖回蓝色；`fallbackHue`（heroes.ts:155-160）无调用者；heroId 为 null 的新增卡不会进入 seats 列表（seatdirs.ts:61；webapi.ts:360-363）；`WorkspaceSummary` 无 cwd，目录次级分组不存在。
- 用户感受：进那刻夏席和进昔涟席，除了右下角底纹图不同，卡片一模一样，看不出谁承办。
- 来源：00 §0.1 行22、§1.1 行86（M2 ⑥ 明列）、§6.4 行634；05 §3 第8条 行70
- 建议里程碑：**M2**

**G6 · 回复不流式：卡片最快 1s 后才刷新** — WB-49／WB-13／WB-40／WB-01（子项）
- 状态：WB-49 缺失；WB-13 偏离
- 差在哪：宿主页无 `session.subscribe`/`state.partial.blocks`/`useConversation`，从不发 `amphoreus:live-reply`（src/client 0 命中）；iframe 侧 `app.js:1890-1954` 的 live 渲染链路永不触发；正文实际靠 `app.js:1962` 每秒 `pollProjection` 拉宿主投影。
- 用户感受：在画布里追问，回复过程中卡片一动不动，回合结束 + 800ms 落盘 + 1s 轮询后才整段出现。
- 来源：00 §5.4 行572、§6.2 行611/614；HANDOFF 行108（「正文由宿主页 useConversation 喂入」列 M2）
- 建议里程碑：**M2**

### 第二梯队：功能缺口，用得越多越明显

**G7 · 分支出的新会话不继承席位** — WB-24／WB-38／WB-27（部分）
- 状态：WB-24 缺失
- 差在哪：设计要 H3 在 `session/created` 时「父有绑定→子写 `fork-inherit` pending」；`src/index.ts:77` 的 session/created 只做投影回放，`injector.ts` 全文不监听它；`'fork-inherit'` 仅存在于枚举（store.ts:35）。核查方指出：缺失点应定位在 host injector，而非 `workbench.tsx` 的 fork 分支。
- 用户感受：在席内点「分支」，新会话掉进「全体会议」或靳靳靠 cwd 前缀匹配，且不会注入该英雄的技能卡。
- 来源：00 §5.3 H3 行555、§1.1 行86
- 建议里程碑：**M2**

**G8 · 会话正文落盘宿主、经无令牌路由外露** — WB-15／WB-63／WB-03／WB-36／WB-16／WB-62
- 状态：偏离
- 差在哪：`workbench.ts:7` 自述 persists projected message text；`:18-19, 599-604` 宿主侧 8000 字截断；`:444-455` 正文入 `thread.messages` 随 `#save` 写 `workbench.json`；`:480-509` 工具参数/结果全文入 process；`webapi.ts:393-398` GET workspaces/:id 无凭据返回全部；`config.ts:80-81` 的 `cardTextLimit` 无任何消费者（死配置）；`NOTICE:7` 却写「session bodies no longer persisted host-side」，与代码相反。设计要求的索引形状 `{cards:[{turnIndex,userSeq,assistantSeq,toolCallIds…}],forks:[…]}` 不存在。
- 用户感受：改 `cardTextLimit` 无效；`$DSH_HOME/amphoreus/workbench.json` 里存着对话正文副本（含注入的技能卡全文）；本机任意进程可读。
- 来源：00 §2.7 SEC-1 行259、§5.3 H8 行560、§6.2 行614、§6.4 行637（明确否决「保留并加 nonce 门」）；HANDOFF 行104/108
- 建议里程碑：**M2（改动最大的一项，建议紧随 G6 一起做——正文改由宿主页喂入后，宿主投影自然可以瘦身为索引）**

**G9 · `defaultView` 无效、切会话不记住 Tab** — WB-14／WB-60
- 状态：缺失
- 差在哪：`config.ts:80` 定义了 `defaultView`，全 src 无消费者；无 `ctx.uiConversation.binding(id).activate('amphoreus-workbench')`；无 `dsh-amphoreus:workbench-tab` 键。
- 用户感受：设 `defaultView:'workbench'` 没反应；每次切会话都回到「对话」Tab。
- 来源：00 §3.1 行373、§4.5 行462、§5.4 行572
- 建议里程碑：**M2**

**G10 · 关掉 `workbench.enabled` Tab 仍在；投影失败无标记；设置区不显示原因** — WB-35／WB-70／WB-20
- 状态：部分
- 差在哪：`src/client/index.ts:62-83` 无条件注册 Tab，`webapi.ts:256-262` effectiveConfig 不透传 workbench 字段，客户端无从得知开关；投影失败只 `logger.warn`（index.ts:71-72, 92-93），无「不可投影」标记；`settings.tsx:44-47, 129-146` 只显示 suite 级别与 diagnostics。
- 用户感受：关掉开关后「工作台」Tab 还在，点进去是 503 红条；某会话日志坏了就静默缺席；设置页看不出为什么。
- 来源：00 §3.1 行371、§7.4 行721、§1.3 行111
- 建议里程碑：**M2**

**G11 · 卡片位置/折叠/快捷词只存浏览器** — WB-17／WB-64
- 状态：部分（服务端域、schema、`PUT /amphoreus/api/canvas/:id` 全齐，客户端一行没接）
- 差在哪：`app.js:27,33,42,48` 读、`:82,86,90,94` 写 localStorage；全文无 `/amphoreus/api/canvas` 调用；`quick-phrases` 首启拷入 `global.prefs.quickPhrases` 未做。
- 用户感受：换浏览器、清缓存，画布布局全丢。
- 来源：00 §4.3 行445、§4.5 行462、§6.4 行636
- 建议里程碑：**M2**

**G12 · 「在 DSH 中打开」不定位到该轮** — WB-55／WB-40
- 状态：部分
- 差在哪：`app.js:1796` 发 `{sessionId, seq}`，`workbench.tsx:102-105` 只 `sessions.open(sessionId)`，`seq` 声明于 :38 但从不读取；无 `data-chat-anchor-key` 滚动。
- 来源：00 §6.2 行612/623
- 建议里程碑：**M2**

**G13 · 席内新建会话顺序与 D-E 相反、字段名不同、失败静默** — WB-58／WB-12
- 状态：偏离
- 差在哪：`workbench.tsx:73` 先 `sessions.create` 再 `:76-79` bindSeat 并 `.catch(()=>undefined)`；设计要求先占 `session-<uuid>` → PUT bindings → create，并点名「先 create 再 PUT」为不选项（00:697）；桥接字段是 `seatHeroId` 而非 `skillName`；create 后未 `open`。`injector.ts:69-93` 的 pre-step 兜底能缓解但不等价。
- 用户感受：多数时候能绑上，但存在首条消息漏注入的竞态，绑定失败无提示。
- 来源：00 §6.2 行612、行697；HANDOFF 行29
- 建议里程碑：**M2**

**G14 · 会话列表/席位不走桥接，经宿主 HTTP 轮询** — WB-48／WB-12／WB-16／WB-62
- 状态：偏离
- 差在哪：`amphoreus:workspaces` 宿主页从不发送（app.js:1862-1866 接收端是死代码）；列表与谱系由 iframe `GET /amphoreus/workbench/api/workspaces` 取得；每会话 `skillName/face` 不在载荷中；`SessionSummary.parentId` 未用。核查注：00:560 与 00:613 在「谱系走不走宿主路由」上有内部张力，但 workspaces 消息未发送与 skillName/face 缺失是确定的。
- 建议里程碑：**M2（与 G8 一起处理）**

### 第三梯队：架构/合规/预留项，用户不直接感知

**G15 · 跨会话总览放错位置** — WB-33 · 偏离
设计要求门户走 `sidebar.footer.action` → `shell.overlay` 全屏层，不放 conversation.view；实际门户「全体会议 + 13 席」在 Tab 的 iframe 内（app.js:1277-1314），无 footer/overlay 注册（src 0 命中）。用户必须先选个会话、再切 Tab 才能看总览。来源：00 §6.4 行632、行569/577。里程碑：M2。

**G16 · `.lock` 自写锁与 `amphoreus_canvas` 域「两套真相」** — WB-39 · 部分
`workbench.ts:20-21, 338-347` 仍以 `.lock`/60s/800ms 写 `dataDir/workbench.json`；设计 00:491 把「自写 JSON + .lock」列为不选，00:622 要求 M2 起由 storage-domain 接管。里程碑：M2（与 G8 合并）。

**G17 · `workbench.host` 键名未预留** — WB-09／WB-61 · 部分
`config.ts:78-83` 只有 enabled/defaultView/cardTextLimit/autoProjection。设计 00:372/605 要求第一版就预留。里程碑：M2（一行 schema）。

**G18 · 署名不全** — WB-07／WB-43 · 部分
NOTICE、app.js/styles.css 头注释合规；缺：设置区底部「工作台源自 dsh-synapse（MIT）」一行（settings.tsx 0 命中）、README「致谢」节与「改造版」定位（README.md:40 只在「边界」节一句）、mark.svg 无头注释。核查修正：NOTICE 版权行无作者名是上游原文，**不算缺陷**。来源：00 §6.5 行643-644（B9 署名属 M1）。里程碑：M1 补欠。

**G19 · 旧 synapse 数据迁移未写** — WB-18／WB-65 · 缺失
`store.ts:94` 只有 `synapseMigratedFrom` 字段占位；无首启检测 `$DSH_HOME/synapse/workspaces.json`、无 position 折入。本机目前无该文件，但设计写的是「若存在」检测。里程碑：M2 低优先。

**G20 · M1 原样接入验证无记录** — WB-05／WB-41 · 缺失
HANDOFF、profile web 的 package.json/pnpm-lock 均无 dsh-synapse 安装/卸载痕迹；「哪些 synapse:* 消息仍工作」无清单。设计把「跳过验证直接改造」列为不选（00:603）。已被 vendoring 事实上绕过；建议补一份事后核对记录而非回头重装。里程碑：M1 补欠（文档）。

**G21 · 杂志 `full` 档零实现** — WB-29 · 缺失；WB-31 · 缺失（[推断]，待 4.12 裁决）
`magazineMode` 仅 `config.ts:57` 与 `webapi.ts:258` 透传，client/workbench 0 命中；Q&A 页、栏目页、页码、trajectory 折叠等均无。00:360/86 与 05:92 都把 full 起步放在 M2。WB-31 的 13 册栏目模板尚未被裁决为硬目标。里程碑：M2 后段。

### 第四梯队：M3 项，按计划尚未开工（列出以便对账，不计入当前缺口）

| ID | 内容 | 现状要点 |
|---|---|---|
| WB-25／WB-68 | 画布 `handoff` 虚线移交边 | `canvasConnectors`（app.js:890-903）只有父子实线与草稿虚线；`observations` 表只有 schema 与只读 entries，无写入者；`src/host` 无 observer.ts |
| WB-26 | B8 站位轨（`conversation.session.header.utilities`） | 无槽注册；客户端无 pipelines 消费者 |
| WB-27 | 卡片「移交」操作 + fork 后 PUT bindings（U5 后半） | 只有追问/分支；`BindInput` 支持 `handoff`/`handoff-fork` 但无调用者 |
| WB-28 | 会诊 ≤3 卡并列、「未部署」站位、移交横条 | 「未部署」只在门户卡（app.js:1287,1294） |
| WB-30 | 「接通中」尾页 | 无运行时识别 `此事移交◯◯：`（parse.ts 仅解析技能卡静态契约） |
| WB-59 | `amphoreus:accept-handoff` 全链路 | 消息、弹层、`POST handoffs/:id/:seq/accept`、open 全缺 |
| WB-34 | **⚠ 核查有分歧** markdown 保留（done）+ B10 台账（缺） | **维持方**：条目含 B10，B10 零实现→partial；**反驳方**：B10 与 native AssistantMarkdown 同属 M3，判定者对两者尺度不一致，当前里程碑应记 done、B10 留 M3 验收。两方都同意 B10 目前没有 |

## 4. 推荐的下一步顺序

1. **过滤注入消息，不投影成卡（G1）**
   验收：进任一席新建会话并发一句话，画布只出现这一张卡，标题不含 `<system-reminder>`／`<skill_content>`；`workbench.json` 中亦不含该注入文本。

2. **打通主题 token 桥（G2）**
   验收：宿主页在 `theme/change` 时发 `amphoreus:theme-tokens {tokens, dark}`；`styles.css` 第一步变量化（`var(--dsw-alias-bg-base, #f5f7fa)` 带回退）后，切 DSH 暗色 iframe 同步变暗，进不同席画布底色随席色变化；`--dsw-alias-*` 在 styles.css 中出现 ≥ 主要色位数（bg/text/border/accent）。

3. **画布写请求补 nonce（G3）+ 「对话」按钮切回 Tab（G4）+ 预留 `workbench.host` 键（G17）**
   验收：点「归档」不再报 `invalid amphoreus nonce`；点 iframe 内「对话」回到官方对话 Tab（或该切换条被隐藏且 WB-45 争议随之消解）；`config.ts` 出现 `host: 'iframe' | 'native'` 默认 `'iframe'`。

4. **正文改由宿主页喂入，宿主投影瘦身为索引（G6 + G8 + G14 + G16）**
   验收：`workbench.tsx` 以 `session.subscribe` 读 `state.partial.blocks` 发 `amphoreus:live-reply`，追问时卡片逐字更新；`workbench.json` 不再含 `messages[].text`，`GET /amphoreus/workbench/api/workspaces/:id` 返回体无正文；`cardTextLimit` 在浏览器侧生效（改成 1000 能看到截断）；`.lock` 文件不再出现。

5. **卡片黄金裔身份 + fork 继承席位（G5 + G7 + G13）**
   验收：每张卡显示承办名牌与徽记（不只靠颜色），席色边框来自 `bindings`；从席内分支出的会话在 `bindings` 表出现 `source:'fork-inherit'` 记录且首轮注入了对应技能卡；席内新建走「先 PUT 再 create」，绑定失败在 iframe 有可见错误。

6. **收尾 M2 小项（G9–G12、G15、G18）**
   验收：`defaultView:'workbench'` 生效且切会话记住 Tab；`workbench.enabled=false` 时 Tab 不出现；投影失败的会话在画布标「不可投影」并在设置区列原因；卡片位置写入 `$DSH_HOME/storages/amphoreus_canvas/canvas/<sessionId>.json`；「在 DSH 中打开」滚到对应轮；设置区底部出现「工作台源自 dsh-synapse（MIT）」；README 有「致谢」节。

## 5. 附：审计覆盖范围

**覆盖的代码**（均为 `D:/DeepSeek Harness/deepseek插件开发/dsh-amphoreus/` 下）：
`workbench/app.js`、`workbench/styles.css`（含 mark.svg 目录核对）；`src/client/` 的 workbench.tsx、index.ts、theme.ts、state.ts、garnish.ts、brand.tsx、settings.tsx、locales.ts；`src/host/` 的 workbench.ts、webapi.ts、injector.ts、seats.ts、seatdirs.ts、store.ts、bridge.ts、firstframe.ts、config.ts；`src/index.ts`；`src/shared/api.ts`、`src/shared/heroes.ts`；以及 NOTICE、LICENSE、README.md、package.json、HANDOFF.md。构建产物 `lib/index.js`、`lib/client.js` 只做了 grep 交叉验证。

**覆盖的设计文件**：`设计底账/05_假设与未决.md`、`06_杂志分册版式底账.md`、`02_dsh-synapse调研.md`、`设计文档/00_总体架构（草稿·未核查）.md`、`dsh-amphoreus/HANDOFF.md`。

**浏览器实测**（localhost:3181→3080，lib 构建于 12:24）：门户首屏、进昔涟席、画布外观、注入卡片、双层切换条、console 无错误、DSH 切暗色 iframe 不跟随。

**没覆盖的**：
- `src/host/suite/*`（parse/reader/roots/watch 等技能解析层）只在核查移交契约时局部翻阅，未整体审计；`tests/` 未审。
- 官方 DSH 宿主源码只为核实 WB-51 读了 ConversationSession 与 scoped-slots 的 `only` 过滤，其他官方 API 行为均以 HANDOFF 记录为准。
- 设计底账 01/03/04 与 `reference/` 目录（除 SYNAPSE-LICENSE.txt）未纳入。
- 未做多浏览器/清缓存后的布局丢失实测、未做多进程并发写 `.lock` 实测、未做性能测量；「写操作 403」为代码推断（api() 无 nonce 头 + #authorize 强制校验），未在浏览器点击复现。
- M3 项（移交、站位、台账、接通中）因数据源 `observations` 从未被写入，无法做运行时实测，仅确认代码缺席。